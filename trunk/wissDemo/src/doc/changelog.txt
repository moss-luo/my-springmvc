2013-1-13
fields加入参数initData，在springmvc中，接收参数的bean对象属性类型为int等非String类型时，无法接收""，将出现format失败的错误。
当接收参数的bean对象属性类型为int等非String类型时,fields中类型为select的必须指定initValue,其他类型必须指定相对应的field的initData（默认值）；
2013-1-15
修改了搜索框重置的实现方法，原来方法无法将select选择框正确重置；
2013-1-16
修复CRUD新增和修改时，select表单域无法选中默认值（修改时为当前值）的问题；